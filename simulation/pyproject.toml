[project]
name = "after-economy-sim"
version = "0.1.0"
description = "Allocation under constraint with and without a price interface; the viability cost of a distorted (financialised) price signal"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26",
    "matplotlib>=3.8",
]
